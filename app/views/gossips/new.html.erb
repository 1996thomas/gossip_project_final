<div class="container my-5 py-5 d-flex flex-column justify-content-center align-items-center">
<h1>Créer un gossip</h1>
<%= form_for @gossip do |f| %>
  <div class="form-group py-2">
    <label>Titre du gossip</label>
    <%=f.text_field :title%>
  </div>

  <div class="form-group py-2">
    <label>Contenu du gossip<label>
    <%=f.text_field :content%>
  </div>

  <div class="form-group py-2">
  <a href="<%= gossips_path%>" class="btn btn-sm btn-outline-primary">Revenir aux gossips</a>
    <button type="submit" class="btn btn-sm btn-primary">Ajouter le gossip</button>
  </div>
<%end%>
</div>